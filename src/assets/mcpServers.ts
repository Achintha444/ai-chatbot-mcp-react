/**
 * Fima mcp server unique id
 */
export const figmaMCPServerUniqueId: string = "figmaMCPServerUniqueId";

/**
 * Available mcp servers
 */
export const availableMCPServerUrls: Map<string, string> = new Map([
    [figmaMCPServerUniqueId, "http://localhost:3333"],
]);

